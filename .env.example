# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=freesscan
DB_USER=scanner
DB_PASSWORD=changeme

# PostgreSQL Admin (for initialization)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# API Configuration
API_PORT=8080
PORT=8080

# CI/CD Configuration
SCANNER_API=http://localhost:8080
FAIL_ON_CRITICAL=true
FAIL_ON_HIGH=false
MAX_WAIT=300

# Scanner Configuration
SCAN_TARGET=./test-repos

# Backup Configuration
BACKUP_DIR=./backups
